%h1 Listing songs

%table
  %tr
    %th= "Author - title"
    %th
    %th
    %th

  - @songs.each do |song|
    %tr
      %td
        .ui360= link_to "#{song.author} - #{song.title}", song.content.url
      %td= link_to 'Show', song
      %td= link_to 'Edit', edit_song_path(song)
      %td= link_to 'Destroy', song, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Song', new_song_path
